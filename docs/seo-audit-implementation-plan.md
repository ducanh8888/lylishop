# LyliShop Technical SEO Audit and Implementation Plan

Repository: `ducanh8888/lylishop`

Domain: `https://lylishop.online`

Audit date: 2026-06-29

Scope: Next.js 15 App Router, TypeScript, TailwindCSS, static product showcase. No WordPress, CMS, database, backend, cart, checkout, fake address, fake email, fake reviews, or fake business data should be introduced.

## 1. Executive Summary

LyliShop already has a solid technical SEO base:

- Next.js Metadata API is used.
- Canonical URLs are present on the main public routes.
- `robots.txt` and `sitemap.xml` are generated by App Router metadata routes.
- Product pages are statically generated from `lib/products.ts`.
- JSON-LD exists for Organization, WebSite, Product, FAQPage, BreadcrumbList, ItemList, CollectionPage, and BlogPosting.
- Images are local assets and rendered with `next/image`.
- The site has a clear brand, product collection, footer links, CTA sections, FAQ, and social links.

The original highest-risk issue was trust/data integrity around reviews and ratings. Phase 0 removed the unverified rating/review/testimonial layer from runtime UI, product data, and Product structured data.

The second major issue was strategy alignment around existing `/blog` and `/blog/[slug]` URLs. Phase 5 now keeps blog as secondary informational/lifestyle content with low sitemap priority, while homepage, `/products`, `/moc-khoa-len`, and product detail pages remain the primary SEO surfaces.

The third issue is topic cannibalization. Homepage, `/moc-khoa-len`, `/products`, and blog posts all target overlapping terms around "moc khoa len handmade". The implementation should define one intent per page:

- Homepage: brand/pillar page for LyliShop and "moc khoa len handmade cute".
- `/products`: product collection / commercial listing.
- `/moc-khoa-len`: informational collection page or keyword-focused category guide.
- Product detail pages: product-specific long-tail subpages.
- Blog routes: secondary informational/lifestyle layer, not the primary SEO funnel.

## Phase 0 Final Decision Addendum

Status after Phase 0 final patch:

- The unverified rating/review/testimonial layer is removed from runtime UI, product data, and Product JSON-LD. Product schema must not emit `aggregateRating` or `review` unless real verified review data is added later.
- Product entities now carry a lean set of factual fields: `category`, `tags`, `material`, and `benefits`. Gift suitability is handled as plain page copy instead of a structured product field.
- Related products use simple deterministic logic: same category first, then tag-overlap fallback.
- Existing `/blog` and `/blog/[slug]` URLs are kept as legacy static guide content to avoid breaking live URLs. They remain indexable, but sitemap priority is intentionally lower than homepage, `/moc-khoa-len`, `/products`, and product detail pages. This is not approval to create a new blog program; it is a safe legacy URL strategy until Search Console data supports a redirect/noindex migration.
- Homepage and product/category routes remain the primary SEO surfaces for moc khoa len handmade commercial intent.

## 2. Repository Areas Audited

The following areas were reviewed:

- `app/`
- `components/`
- `lib/`
- `public/`
- `styles/`
- `middleware.ts`
- `next.config.ts`
- `package.json`
- `README.md`
- recent git history
- tests under `tests/`

Special focus files reviewed:

- `app/layout.tsx`
- `app/page.tsx`
- `app/products/page.tsx`
- `app/products/[slug]/page.tsx`
- `app/moc-khoa-len/page.tsx`
- `app/blog/page.tsx`
- `app/blog/[slug]/page.tsx`
- `app/robots.ts`
- `app/sitemap.ts`
- `lib/site.ts`
- `lib/products.ts`
- `lib/schema.ts`
- `lib/seo.ts`
- `lib/faq.ts`
- `lib/testimonials.ts`
- `public/product-assets/`
- `public/gallery/`

Note: the prompt mentions `public/products/`, but the current repository does not contain that directory. Product images currently live in `public/product-assets/`. `README.md` still references `public/products/`, so documentation is out of sync.

## 3. Current Product Inventory

`lib/products.ts` currently defines 8 products:

1. `crochet-bear-keychain` - `Moc khoa len tho con`
2. `strawberry-yarn-charm` - `Moc khoa len dau tay`
3. `tulip-crochet-keychain` - `Moc khoa len hoa tulip`
4. `bunny-plush-charm` - `Moc khoa thu len tho bong`
5. `cat-paw-crochet-accessory` - `Moc khoa len ban chan meo`
6. `set-moc-khoa-len-6-mau-cute` - `Moc khoa thu mini size S dong gia 45k`
7. `set-moc-khoa-len-6-mau-qua-tang` - `Mau moc khoa dong gia 45k`
8. `set-moc-khoa-len-10-mau-mix` - `10 ma san pham duoc yeu thich nhat Lyli Shop`

Status: product count requirement is satisfied. No placeholder products are needed.

Concern: slug `crochet-bear-keychain` does not match the visible product name `Moc khoa len tho con`. Because this URL may already be indexed, do not rename it without a redirect plan.

## 4. Live Route Checks

Live checks on `https://lylishop.online` showed:

| URL | Status | Notes |
| --- | ---: | --- |
| `/` | 200 | Homepage is live. |
| `/products` | 200 | Product collection is live. |
| `/moc-khoa-len` | 200 | Keyword collection page is live. |
| `/blog` | 200 | Existing blog/cam nang route is live. |
| `/robots.txt` | 200 | Allows public crawling and points to sitemap. |
| `/sitemap.xml` | 200 | Includes homepage, products, moc-khoa-len, blog, privacy, terms, product pages, blog posts. |
| `www.lylishop.online/` | 308 | Redirects to apex `https://lylishop.online/`. |
| `/not-a-real-page` | 404 | Not found behavior is correct. |

## 5. Audit Scorecard

| Area | Status | Summary |
| --- | --- | --- |
| Technical SEO | WARNING | Good foundation, but Twitter metadata inheritance, blog sitemap strategy, internal link targets, and route intent separation need cleanup. |
| Product SEO | PASS / WARNING | Product pages now use a lean model with `category`, `tags`, `material`, `benefits`, and simple related products. Slug mismatch should still not be changed without redirects. |
| Homepage SEO | PASS / WARNING | Has one H1, CTA, FAQ, and long SEO content. Needs clearer page intent versus `/moc-khoa-len`. |
| Schema | PASS / WARNING | Schema coverage is useful and Product JSON-LD is now lean. Keep it factual and do not reintroduce reviews/ratings without verified visible data. |
| Semantic SEO | WARNING | Topic cluster exists but is not cleanly governed. |
| Entity SEO | PASS / WARNING | Brand, domain, footer, social links, sameAs are mostly consistent. No fake address/email found. |
| Accessibility | WARNING | Skip link, alt text, ARIA labels mostly exist. Skip link localization and mobile contrast can still be polished. |
| Performance | PASS / WARNING | Local images, `next/image`, cache headers, and server components are good. OG/Twitter assets and duplicate thumbnails can be improved. |
| AI Optimization | WARNING | Good structured data and context, but data trust and topic overlap reduce machine-readability. |

## 6. Technical SEO Audit

### 6.1 Metadata

Status: WARNING

Findings:

- Global metadata is centralized in `lib/seo.ts` and used from `app/layout.tsx`.
- Homepage, product collection, keyword collection, blog, privacy, and terms define route metadata.
- Product pages use `generateMetadata`.
- `/products`, `/privacy`, and `/terms` define OpenGraph but do not define route-specific Twitter metadata, so Twitter tags inherit global homepage title/description.
- `keywords` are still present in global metadata and some routes. This is not harmful by itself, but modern SEO value is limited.

Recommended actions:

- Add route-specific Twitter metadata to all public routes with unique page intent.
- Keep canonical, title, description, OpenGraph, Twitter, and robots consistent through a helper.
- Avoid keyword stuffing in metadata.
- Optionally remove or reduce `keywords` if the team wants a cleaner metadata strategy.

### 6.2 Canonical

Status: PASS

Findings:

- Homepage canonical resolves to `https://lylishop.online`.
- `/products` canonical resolves to `https://lylishop.online/products`.
- `/moc-khoa-len` canonical resolves to `https://lylishop.online/moc-khoa-len`.
- Product pages canonicalize to their own product URLs.

Recommended actions:

- Keep canonical URLs stable.
- Do not change product slugs without redirect analysis.

### 6.3 Robots

Status: PASS

Findings:

- `app/robots.ts` allows all public pages.
- Sitemap is declared.
- Host is set to `SITE.url`.

Recommended actions:

- Keep `Allow: /`.
- If `/blog` is later removed from strategy, decide whether to keep indexed, noindex, or redirect at route level rather than blocking in robots.

### 6.4 Sitemap

Status: WARNING

Findings:

- `app/sitemap.ts` includes homepage, `/products`, `/moc-khoa-len`, `/blog`, `/privacy`, `/terms`, all product pages, and all blog posts.
- Sitemap uses `new Date()` for `lastModified`, which changes every build. This can be acceptable but is not semantically accurate if content has not changed.
- Sitemap includes blog URLs, conflicting with the no-blog requirement unless the team intentionally treats current `/blog` as legacy/static guide content.

Recommended actions:

- Keep only intentional indexable routes in sitemap.
- Add stable `updatedAt` fields to product/blog/content data if accurate timestamps are needed.
- Do not remove blog URLs before checking index status and traffic.

### 6.5 Status Codes and Redirects

Status: PASS

Findings:

- `middleware.ts` redirects `www.lylishop.online` to `lylishop.online` with 308.
- Main live routes return 200.
- Unknown route returns 404.

Recommended actions:

- Keep apex domain as canonical.
- If product slug is renamed, add 308 redirects from old URLs to new URLs.

### 6.6 URL Structure

Status: WARNING

Findings:

- Product URLs use `/products/[slug]`.
- `/moc-khoa-len` is a strong keyword URL.
- `/blog` exists, despite the no-blog strategic requirement.
- Some product slugs are English while visible names are Vietnamese, e.g. `crochet-bear-keychain` for a rabbit product.

Recommended actions:

- Do not change existing URLs immediately.
- If changing slugs for SEO, create redirects and validate impact in Google Search Console.
- Prefer stable Vietnamese/ASCII slugs for future products.

### 6.7 Breadcrumb

Status: PASS / WARNING

Findings:

- Breadcrumb UI and JSON-LD exist for `/products`, `/moc-khoa-len`, product detail, and blog.
- Product detail breadcrumb uses `Trang chu > Moc khoa len > Product`, while CTA also links back to `/products`. This is acceptable but could be clearer.

Recommended actions:

- Decide whether product detail parent should be `/products` or `/moc-khoa-len`.
- Align visible breadcrumb, JSON-LD breadcrumb, and internal link strategy.

### 6.8 Internal Links

Status: WARNING

Findings:

- Header link "San pham" points to `/#featured` instead of `/products`.
- Footer has `/products` and `/moc-khoa-len`.
- Homepage links to `/products`, `/blog`, blog posts, Zalo, Facebook.
- `/products` links to `/moc-khoa-len` and `/blog`.
- Product pages link to `/products`, `/moc-khoa-len`, and related product pages.

Recommended actions:

- Change main navigation "San pham" to `/products`.
- Keep a separate "Mau noi bat" anchor if needed for homepage section.
- Use natural anchors: "moc khoa len handmade", "moc khoa len cute", "moc khoa len theo yeu cau", "xem cac mau moc khoa len".

### 6.9 OpenGraph and Twitter

Status: WARNING

Findings:

- Global OpenGraph/Twitter exists.
- Homepage has specific OpenGraph/Twitter.
- `/products` has specific OpenGraph but inherits homepage Twitter.
- `/privacy` and `/terms` have specific OpenGraph but inherit homepage Twitter.

Recommended actions:

- Add route-specific Twitter metadata for all important public routes.
- Keep OG image local.
- Optimize `og.png` and `twitter-card.png` size if visual quality allows.

## 7. Product SEO Audit

Status: WARNING

Good:

- 8 products exist.
- Each product has name, slug, price, short description, long description, meta description, image source, alt, width, height, highlights.
- Product detail pages have H1, description, highlights, CTA, FAQ, related products.
- Product images are local and rendered with `next/image`.

Remaining issues:

- Product slug `crochet-bear-keychain` does not match the product name "tho con".
- Product data should stay lean; do not add gift/review/rating fields unless real visible data exists.

Recommended actions:

- Keep verified product fields only when data is real.
- Keep related products simple: same category first, then tag overlap fallback.
- Avoid slug changes unless redirect plan is approved.

## 8. Homepage SEO Audit

Status: PASS / WARNING

Good:

- Homepage has one H1.
- H1 contains "moc khoa len handmade".
- Hero has clear CTA.
- Homepage has product section, reasons section, SEO text, gallery, testimonials, FAQ, contact CTA, final CTA.
- FAQ schema matches visible FAQ.
- Organization, WebSite, WebPage, ItemList, FAQPage JSON-LD are rendered.

Issues:

- Homepage also links heavily to `/blog`, which conflicts with the no-blog requirement unless current blog is intentionally retained.
- Homepage and `/moc-khoa-len` overlap heavily on "moc khoa len handmade".
- Testimonials section may be unsafe if reviews are not verified.

Recommended actions:

- Keep homepage as brand/pillar page.
- Keep `/products` as commercial collection.
- Decide whether `/moc-khoa-len` is an informational category page or whether its best content should move into homepage/products.
- Remove testimonial/rating content if not verified.

## 9. Schema Audit

| Schema | Current status | Recommendation |
| --- | --- | --- |
| Organization | PASS | Keep. Do not add address/email. Keep sameAs only for real social URLs. |
| WebSite | PASS | Keep. Do not add SearchAction because there is no real search feature. |
| Product | WARNING | Keep core fields. Verify offers/prices. Remove aggregateRating if not verified. |
| FAQPage | PASS | Keep only where FAQ is visible. |
| BreadcrumbList | PASS | Keep and align with visible breadcrumb. |
| ItemList | PASS | Keep for homepage and collection pages. |
| Article / BlogPosting | WARNING | Keep only if `/blog` remains an intentional indexed section. |
| HowTo | N/A | Do not add without true step-by-step HowTo content. |
| Review | N/A / FAIL if fake | Do not add unless real reviews exist and are displayed. |
| AggregateRating | FAIL if unverified | Remove unless rating/reviewCount are real and verifiable. |
| ImageObject | PASS / WARNING | Existing logo and primary image objects are okay. Can be improved for page-specific images. |
| VideoObject | N/A | Do not add without real videos. |
| CollectionPage | PASS / WARNING | Present for `/moc-khoa-len`; consider adding for `/products`. |

## 10. Semantic SEO Audit

Status: WARNING

Findings:

- Topic cluster exists but is not fully governed.
- Homepage, `/moc-khoa-len`, `/products`, and blog posts overlap on similar terms.
- `/moc-khoa-len` is a strong keyword landing page with semantic sections and FAQ.
- `/products` is a product collection but includes links to guide/blog content.

Recommended topic model:

- Pillar: homepage targets brand + "moc khoa len handmade cute".
- Collection: `/products` targets purchase/listing intent.
- Informational category: `/moc-khoa-len` targets "moc khoa len la gi / cach chon / qua tang".
- Subpages: product detail pages target product-specific long tails.
- Existing blog: either retain as static guide content or phase out with noindex/redirect after index analysis.

## 11. Entity SEO Audit

Status: PASS / WARNING

Good:

- `SITE.name` is `LyliShop`.
- Domain is `https://lylishop.online`.
- Footer repeats brand description.
- Header/footer include social/entity links.
- Organization sameAs includes Facebook, TikTok, Instagram.
- No fake address or email appears in site config.

Issues:

- No standalone About/Contact route exists. This is not mandatory, but could help entity clarity if content is real.
- README still references old image directory and Framer Motion, while runtime wrapper no longer imports Framer Motion.

Recommended actions:

- Do not create fake address, fake email, fake Google Business Profile.
- Consider only real About/Contact content if the business owner wants entity depth.
- Update documentation to match current assets and architecture.

## 12. Accessibility Audit

Status: WARNING

Good:

- Skip-to-content link exists.
- CTA links have visible text.
- Social icon links have aria-labels.
- Product/gallery images have alt text.
- FAQ uses native `details`/`summary`.
- Focus-visible styling exists globally.

Issues:

- Skip link text is English: "Skip to content".
- Contrast should be checked visually on mobile after changes.

Recommended actions:

- Localize skip link.
- Keep native controls.
- Validate keyboard navigation on mobile menu and FAQ.

## 13. Performance and Core Web Vitals Audit

Status: PASS / WARNING

Good:

- Most components are Server Components.
- `components/motion/Reveal.tsx` is currently a pass-through wrapper and does not import Framer Motion.
- Images use `next/image`.
- Hero image uses `priority`.
- Non-hero images generally lazy-load.
- `next.config.ts` enables AVIF/WebP output.
- Static asset cache headers are configured.
- Analytics is delayed after load.

Issues:

- `README.md` still mentions Framer Motion even though dependency is not present in `package.json`.
- `og.png` and `twitter-card.png` are about 626 KB each.
- Several original JPG files are large, though some are not referenced by code.
- Product detail page can duplicate the main image in thumbnail list.
- Local SVG logos are large around 217 KB each.

Recommended actions:

- Keep no new libraries unless necessary.
- Optimize social preview images.
- Avoid loading duplicate product image thumbnails.
- Keep using local assets, not remote images.

## 14. AI Optimization Audit

Status: WARNING

Good:

- Semantic HTML sections are present.
- JSON-LD gives machines organization, website, product, FAQ, breadcrumb, and collection context.
- Internal links provide contextual relationships.
- Brand entity is repeated in header/footer/schema.

Issues:

- Rating/review trust risk can reduce quality for AI/search systems.
- Blog/no-blog conflict creates ambiguous content architecture.
- Overlapping page intent can make the most authoritative URL unclear.

Recommended actions:

- Prioritize factual consistency.
- Keep schema strictly aligned with visible content.
- Make page intent and internal links explicit.
- Avoid fake reviews, fake business data, or unsupported claims.

## 15. Implementation Plan

### Phase 0: Data Integrity and Index Strategy

Goal:

Confirm which data is real and decide what to do with existing `/blog` URLs.

Reason:

SEO improvements are risky if schema and displayed content include unverified reviews, ratings, prices, or promotions. Existing indexed URLs should not be removed without data.

Files likely changed:

- `lib/products.ts`
- `lib/testimonials.ts`
- `lib/promotions.ts`
- `lib/schema.ts`
- `components/ProductCard.tsx`
- `components/sections/TestimonialsSection.tsx`
- `app/sitemap.ts`
- `app/blog/page.tsx`
- `app/blog/[slug]/page.tsx`
- `lib/blog.ts`

Risk:

Medium, because removing rating/review content may change social proof and rich result eligibility.

SEO impact:

High. This phase improves trust and avoids structured data policy risk.

UX impact:

Medium. Removing testimonials/ratings changes perceived credibility unless replaced with verified facts.

Performance impact:

Neutral.

Migration needed:

No database migration. Content-only changes.

Redirect needed:

Only if `/blog` or blog post URLs are removed or consolidated.

Google index impact:

Potentially high if blog URLs are noindexed or redirected.

Tasks:

| Task | Priority | Complexity | Risk | Expected impact |
| --- | --- | --- | --- | --- |
| Confirm phone, Zalo, Facebook, TikTok, Instagram are real and current. | P0 | XS | Low | High |
| Confirm each product price is real and current. | P0 | S | Low | High |
| Confirm rating, reviewCount, and testimonials are real customer data. | P0 | S | Medium | High |
| If reviews are not verified, remove rating/review UI and `aggregateRating`. | P0 | M | Medium | High |
| Decide whether existing `/blog` remains indexed static guide content. | P0 | M | High | High |
| Check Google Search Console performance/index data before removing blog URLs. | P0 | M | Medium | High |

### Phase 1: Metadata and Route SEO Normalization

Goal:

Make every public route have consistent title, description, canonical, OpenGraph, Twitter Card, and robots metadata.

Reason:

Some routes inherit homepage Twitter metadata, and metadata logic is duplicated across pages.

Files likely changed:

- `lib/seo.ts`
- `app/layout.tsx`
- `app/page.tsx`
- `app/products/page.tsx`
- `app/products/[slug]/page.tsx`
- `app/moc-khoa-len/page.tsx`
- `app/privacy/page.tsx`
- `app/terms/page.tsx`
- `tests/seo-metadata.test.ts`

Risk:

Low.

SEO impact:

High.

UX impact:

Low.

Performance impact:

Neutral.

Migration needed:

No.

Redirect needed:

No.

Google index impact:

Positive. Search snippets and social previews become clearer.

Tasks:

| Task | Priority | Complexity | Risk | Expected impact |
| --- | --- | --- | --- | --- |
| Add a route metadata helper for title, description, canonical, OG, Twitter. | P1 | M | Low | High |
| Add route-specific Twitter metadata to `/products`, `/privacy`, `/terms`. | P1 | S | Low | Medium |
| Ensure descriptions are natural and roughly 140-160 characters where possible. | P1 | S | Low | Medium |
| Keep public robots index/follow. | P1 | XS | Low | Medium |
| Add tests for route-level metadata where practical. | P2 | S | Low | Medium |

### Phase 2: Lean Schema Cleanup Only

Goal:

Keep structured data clean, factual, and easy to audit without building a schema governance system.

Reason:

Small static ecommerce sites get the most SEO value from simple, accurate JSON-LD that matches visible page content. Unsupported review/rating data and hidden enrichment fields create policy and trust risk.

Files changed:

- `lib/schema.ts`
- `app/products/page.tsx`
- `app/moc-khoa-len/page.tsx`
- `tests/seo-metadata.test.ts`

Risk:

Low.

SEO impact:

High. Cleaner structured data improves machine understanding and avoids unsupported rich-result claims.

UX impact:

None. Schema changes do not alter visible UI.

Performance impact:

Neutral to slightly positive because JSON-LD payloads are smaller.

Migration needed:

No.

Redirect needed:

No.

Google index impact:

Positive for trust and clarity. Rich-result display may become simpler because no review/rating data is emitted.

Implemented status:

- Product JSON-LD now emits only supportable basics: `name`, `description`, `image`, `brand`, and valid `offers`.
- Product schema no longer emits `aggregateRating`, `review`, `additionalProperty`, derived trust fields, category, material, benefits, or gift suitability.
- CollectionPage/ItemList schema remains simple and only lists visible product entries.
- Organization keeps real brand/social/contact data only; no address/email/SearchAction was added.
- No schema validation framework or schema regression suite was introduced.

Tasks:

| Task | Priority | Complexity | Risk | Expected impact |
| --- | --- | --- | --- | --- |
| Keep Product JSON-LD limited to visible/supportable basics. | P0 | S | Low | High |
| Keep valid `offers` only while real prices exist in product data. | P0 | XS | Low | High |
| Keep Organization/WebSite/Breadcrumb/FAQ/CollectionPage helpers as simple functions. | P1 | S | Low | Medium |
| Avoid schema governance layers, hidden enrichment, fake reviews, and fake ratings. | P0 | XS | Low | High |

### Phase 3: Minimal Product SEO Model

Goal:

Improve product SEO with the smallest useful product data model.

Reason:

Product pages need enough structure for clear copy, filtering of related items, and Google understanding. They do not need an ontology, semantic graph engine, or recommendation framework.

Files changed:

- `lib/products.ts`
- `app/products/[slug]/page.tsx`
- `tests/seo-metadata.test.ts`

Risk:

Low.

SEO impact:

Medium to high. Product pages now expose clearer category, tags, material, benefits, and related products while staying maintainable.

UX impact:

Medium. Product detail pages remain readable and still show material, benefits, and gift-oriented copy.

Performance impact:

Neutral.

Migration needed:

Type/content-only change. No data migration.

Redirect needed:

No. Product slugs remain unchanged.

Google index impact:

Positive if factual content remains stable. No URL churn.

Implemented status:

- Product type keeps `category`, `tags`, `material`, and `benefits`.
- `giftFor` was removed as a structured field; gift suitability is rendered as normal copy on product detail pages.
- Related products use simple deterministic logic: same category first, then tag-overlap fallback, with a default limit of 3.
- No semantic graph, ontology, or dependency-based recommendation system was added.

Tasks:

| Task | Priority | Complexity | Risk | Expected impact |
| --- | --- | --- | --- | --- |
| Keep `category` required for every product. | P1 | XS | Low | Medium |
| Keep `tags` as a simple string array. | P1 | XS | Low | Medium |
| Keep `material` and `benefits` only because they are visible factual product content. | P1 | S | Low | Medium |
| Keep related products simple: same category first, then tag overlap fallback. | P1 | S | Low | Medium |
| Do not rename product slugs without a dedicated redirect plan. | P0 | XS | Medium | High |

### Phase 4: Simple Silo Internal Linking

Goal:

Clarify site architecture around three core SEO hubs without over-optimization.

Reason:

Google should understand one clear role per core route: brand, commercial collection, and informational guide. This can be done with normal internal links and anchor sections, not scoring systems.

Files changed:

- `app/page.tsx`
- `app/products/page.tsx`
- `app/moc-khoa-len/page.tsx`
- `app/products/[slug]/page.tsx`
- `components/sections/HeroSection.tsx`
- `components/sections/FeaturedProductsSection.tsx`

Risk:

Low.

SEO impact:

High. The main crawl path and page intent are clearer.

UX impact:

Medium. Users get clearer routes to shop, learn, and compare products.

Performance impact:

Neutral.

Migration needed:

No.

Redirect needed:

No.

Google index impact:

Positive through clearer internal linking and intent separation.

Implemented status:

- Homepage links to `/products` and `/moc-khoa-len`.
- `/products` links back to homepage, links to `/moc-khoa-len`, and lists product detail links.
- `/moc-khoa-len` acts as the informational pillar and includes simple anchor navigation for what it is, how to choose, gift usage, and classification.
- Product detail pages link to `/products`, `/moc-khoa-len`, and simple related products.
- No deep multi-page silo system, link scoring system, or automated anchor optimization was added.

Tasks:

| Task | Priority | Complexity | Risk | Expected impact |
| --- | --- | --- | --- | --- |
| Keep `/` as brand/emotional intent. | P1 | S | Low | High |
| Keep `/products` as commercial collection intent. | P1 | S | Low | High |
| Keep `/moc-khoa-len` as informational pillar intent. | P1 | S | Low | High |
| Keep pillar anchors for what it is, how to choose, gift usage, and classification. | P1 | S | Low | Medium |
| Keep product detail internal links to collection, guide, and related products. | P1 | S | Low | Medium |

### Phase 5: Blog Strategy Simplification

Goal:

Remove ambiguity from the blog strategy while preserving existing URLs.

Reason:

The repository already has `/blog` and `/blog/[slug]`. Deleting or redirecting them without Search Console data would add unnecessary URL risk. The lean option is to keep blog as low-priority secondary content and keep primary SEO authority on the three core hubs.

Chosen option:

Option A - keep blog as secondary informational/lifestyle content.

Files changed:

- `lib/blog.ts`
- `app/blog/page.tsx`
- `app/blog/[slug]/page.tsx`
- `app/sitemap.ts`
- `app/page.tsx`
- `app/moc-khoa-len/page.tsx`

Risk:

Low.

SEO impact:

Medium. Blog no longer competes with the primary `/moc-khoa-len` guide and remains useful as supporting content.

UX impact:

Low to medium. Existing blog URLs continue to work.

Performance impact:

Neutral.

Migration needed:

No.

Redirect needed:

No. Existing blog URLs are preserved.

Google index impact:

Stable. Blog remains indexable with low sitemap priority, while homepage, `/products`, `/moc-khoa-len`, and product pages stay primary.

Implemented status:

- Blog is kept as secondary inspiration/lifestyle content.
- Blog metadata and visible copy are softened away from targeting the main "moc khoa len" keyword.
- Blog links softly to `/products` and homepage.
- Sitemap includes `/blog` and blog posts with low priority.
- No per-post index engine, dynamic blog classification system, redirect system, or noindex framework was added.

Tasks:

| Task | Priority | Complexity | Risk | Expected impact |
| --- | --- | --- | --- | --- |
| Keep `/blog` and `/blog/[slug]` live and indexable. | P1 | XS | Low | Medium |
| Keep blog sitemap priority lower than core hubs and product pages. | P1 | XS | Low | Medium |
| Keep blog copy informational/lifestyle, not primary keyword targeting. | P1 | S | Low | Medium |
| Keep soft blog CTAs to `/products` and homepage. | P1 | XS | Low | Medium |
| Do not add per-post index classification or automated blog SEO rules. | P0 | XS | Low | High |

### Phase 6: Image and Core Web Vitals Optimization

Goal:

Improve load performance without changing design significantly.

Reason:

Local image strategy is good, but social images and duplicate thumbnails can be improved.

Files likely changed:

- `app/products/[slug]/page.tsx`
- `components/ProductCard.tsx`
- `components/sections/HeroSection.tsx`
- `components/sections/GallerySection.tsx`
- `next.config.ts`
- `public/og.png`
- `public/twitter-card.png`
- `README.md`

Risk:

Medium if replacing assets.

SEO impact:

Medium.

UX impact:

High.

Performance impact:

High.

Migration needed:

Asset-only migration.

Redirect needed:

No.

Google index impact:

Indirectly positive through Core Web Vitals and image previews.

Tasks:

| Task | Priority | Complexity | Risk | Expected impact |
| --- | --- | --- | --- | --- |
| Avoid rendering first product image twice in detail thumbnails. | P1 | S | Low | Medium |
| Optimize `og.png` and `twitter-card.png`. | P2 | S | Low | Medium |
| Confirm WebP image dimensions and metadata for set products. | P1 | S | Low | Medium |
| Keep `priority` only for hero/LCP image. | P1 | XS | Low | Medium |
| Update README asset directory references. | P2 | XS | Low | Low |

### Phase 7: Accessibility Polish

Goal:

Improve accessibility with small, low-risk changes.

Reason:

Accessibility supports users, search quality, and AI interpretation.

Files likely changed:

- `components/SkipToContent.tsx`
- `components/Navbar.tsx`
- `components/Footer.tsx`
- `styles/globals.css`

Risk:

Low.

SEO impact:

Medium.

UX impact:

High.

Performance impact:

Neutral.

Migration needed:

No.

Redirect needed:

No.

Google index impact:

Indirectly positive.

Tasks:

| Task | Priority | Complexity | Risk | Expected impact |
| --- | --- | --- | --- | --- |
| Localize skip link to Vietnamese. | P2 | XS | Low | Medium |
| Localize/default rating ARIA label or remove rating UI if unverified. | P1 | XS | Medium | High |
| Verify mobile menu keyboard behavior. | P2 | S | Low | Medium |
| Verify contrast on rose/pink sections. | P2 | S | Low | Medium |

### Phase 8: Documentation and Tests

Goal:

Keep implementation safe and easy to audit.

Reason:

SEO regressions are often silent. Tests and docs make changes reviewable.

Files likely changed:

- `README.md`
- `tests/seo-metadata.test.ts`
- `tests/image-assets.test.ts`
- `tests/homepage.test.tsx`
- `tests/products-page.test.tsx`
- existing lightweight route/sitemap tests

Risk:

Low.

SEO impact:

Medium.

UX impact:

Low.

Performance impact:

Neutral.

Migration needed:

No.

Redirect needed:

No.

Google index impact:

Indirectly positive.

Tasks:

| Task | Priority | Complexity | Risk | Expected impact |
| --- | --- | --- | --- | --- |
| Update README to match `public/product-assets/`. | P2 | XS | Low | Low |
| Keep existing lightweight metadata/sitemap checks; do not add a schema testing framework. | P1 | XS | Low | Medium |
| Add product count/image asset tests if not already sufficient. | P2 | XS | Low | Medium |
| Run `npm run test`. | P0 | S | Low | High |
| Run `npm run build`. | P0 | M | Low | High |
| Run lint if script works; if `next lint` is unsupported, migrate script to ESLint CLI. | P1 | S | Low | Medium |

## 16. Suggested Pull Request Breakdown

### PR 1: Trust and Schema Safety

Includes:

- Verify or remove ratings/reviews/testimonials.
- Remove `aggregateRating` if unverified.
- Keep Product JSON-LD minimal and aligned with visible product content.

Why first:

This is the highest-risk SEO compliance issue.

### PR 2: Metadata and Sitemap Cleanup

Includes:

- Route-specific Twitter metadata.
- Metadata helper.
- Sitemap route strategy.
- Robots verification.

Why second:

Low risk, high SEO ROI.

### PR 3: Product Data Model and Detail Pages

Includes:

- Keep category/tags/material/benefits as the minimal product SEO model.
- Keep gift suitability as plain product-page copy, not a structured product field.
- Improve product detail sections.
- Related products by same category first, then tag overlap fallback.

Why third:

Improves long-tail SEO and conversion.

### PR 4: Topic Cluster and Internal Links

Includes:

- Nav link cleanup.
- Homepage/products/moc-khoa-len intent separation.
- Simple three-hub silo: `/`, `/products`, `/moc-khoa-len`.
- Blog as low-priority supporting content.

Why fourth:

Delivers semantic SEO value without adding a deep silo system.

### PR 5: Performance and Accessibility Polish

Includes:

- Optimize social images.
- Avoid duplicate thumbnail image.
- Localize accessibility labels.
- README sync.

Why fifth:

Smaller polish work after core SEO data is safe.

## 17. Overall Roadmap

Phase 0: Data integrity and index strategy

↓ depends on verified real data and GSC/index decision

Phase 1: Metadata and route SEO normalization

↓ depends on phase 0 route strategy

Phase 2: Structured data cleanup

↓ keep schema factual and minimal

Phase 3: Product SEO data model

↓ keep product fields lean and visible

Phase 4: Topic cluster and internal linking

↓ establish three core hubs

Phase 5: Blog strategy simplification

↓ keep existing blog URLs as secondary low-priority content

Phase 6: Image and Core Web Vitals optimization

↓ mostly independent after product image paths are stable

Phase 7: Accessibility polish

↓ independent, can run in parallel with Phase 6

Phase 8: Documentation and tests

Final validation before deployment.

## 18. Open Questions for Owner

1. Are all product prices in `lib/products.ts` real and current?
2. If real customer reviews are added later, where will they be visibly displayed and verified before schema support is reconsidered?
3. Should blog URLs remain secondary inspiration content long term, or should a future Search Console review plan redirects later?
4. Is the product at `/products/crochet-bear-keychain` intentionally a rabbit product despite the bear slug?
5. Is the current promotion still active, or should it be time-bound/removed?
6. Should LyliShop add real About/Contact pages later, without fake address/email?
7. Should product image source of truth remain `public/product-assets/`, replacing old README references to `public/products/`?

## 19. Commands Used During Audit

Read-only commands used:

- `rg --files`
- `git status --short`
- `git log --oneline --decorate -n 20`
- `git log --stat --oneline -n 12`
- `Get-Content -Raw -Encoding utf8 ...`
- `Get-ChildItem -Recurse -File public`
- live `Invoke-WebRequest` checks for route status, robots, sitemap, title, canonical, and JSON-LD types

No source code was modified during the original audit pass.

## 20. Final Recommendation

Start with trust cleanup before adding more SEO content. If ratings and testimonials are not verified, remove them from UI and schema first. Then normalize metadata and sitemap. After that, improve product data structure and internal links.

The implementation should avoid:

- fake reviews
- fake aggregate ratings
- fake Google Business Profile data
- fake address
- fake email
- fake social links
- unsupported product claims
- deleting or changing indexed URLs without redirect/index analysis

This plan is designed so the engineering team can implement changes in small, reviewable pull requests.
